services:
  - type: web
    name: type-of-faith
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: uvicorn server:app --host 0.0.0.0 --port $PORT
    envVars:
      - key: API_BIBLE_KEY
        sync: false
    plan: free
